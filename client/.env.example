# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real secrets.

# Base URL for the Columbus API (no trailing slash)
# Development default is /api/v1 (Vite proxy), so this is only needed for
# staging / production builds that point to a real backend.
VITE_API_URL=http://localhost:3000/api/v1
